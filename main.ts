import hello from "./message.js";
import { Smartphone, call as makeCall } from "./devices.js"

hello();

let phone = new Smartphone("iPhone");
makeCall(phone);

console.log(message);
helloPage();
console.log(sum(5, 7));

tom.print();

for (let point of points) {
    console.log(`Point with coordinates X = ${point.x}, Y = ${point.y}`);
}

let tim = new Person("Timmy", 1);
tim.display();